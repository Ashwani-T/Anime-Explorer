package com.example.animeexplorer.core.domain.enums

enum class AnimeFilter(val filter: String) {
    AIRING("airing"),
    UPCOMING("upcoming"),
    BY_POPULARITY("bypopularity"),
    FAVORITE("favorite")
}